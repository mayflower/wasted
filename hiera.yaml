---
:backends:
  - yaml

:yaml:
    :datadir: /vagrant/vagrant-cfg

:hierarchy:
  - local/%{::environment}/%{::fqdn}
  - local/%{::environment}/%{::hostname}
  - local/%{::environment}/common
  - local/%{::fqdn}
  - local/%{::hostname}
  - local/common
  - '%{::environment}/%{::fqdn}'
  - '%{::environment}/%{::hostname}'
  - '%{::environment}/common'
  - '%{::fqdn}'
  - '%{::hostname}'
  - common

:merge_behavior: deeper
